use system;

const someVar := 50;
const start := system.now();
const test := someVar + (10 - 90 + (-5366 + 5.5)) + (50 * 95 / (5.5)) + 75;
const end := system.now();
system.println("----end----"+(end-start)+" "+test);
