use system:now, println;
//use fibb_sync;

const someVar := 50;
const start := now();
const test := someVar + (10 - 90 + (-5366 + 5.5)) + (50 * 95 / (5.5)) + 75;
const end := now();
println("----end----"+(end-start));
